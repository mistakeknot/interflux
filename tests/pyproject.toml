[project]
name = "interflux-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pytest>=8.0", "pyyaml>=6.0"]

[tool.pytest.ini_options]
testpaths = ["structural", "test_sanitize_untrusted.py"]
pythonpath = ["structural", "../scripts"]
